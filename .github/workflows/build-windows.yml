name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-windows.txt

      - name: Generate icon
        shell: pwsh
        run: |
          python scripts\generate_windows_icon.py

      - name: Build Windows executable
        shell: pwsh
        run: |
          pwsh -ExecutionPolicy Bypass -File scripts\build_windows.ps1

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: separador-de-mandados-PJe-windows
          path: |
            dist\Separador-de-Mandados-PJe.exe
            README-Windows.md

*** Add File: /Volumes/EVO970/Separador de mandado para Windows/Separador de Mandados Windows script  22.03.2026 Themis/Separador de Mandados - Full 20 Completo Themis/scripts/generate_windows_icon.py
from __future__ import annotations

from pathlib import Path

from PIL import Image


def main() -> int:
    project_root = Path(__file__).resolve().parent.parent
    source = project_root / "windows_app" / "assets" / "app_icon.png"
    target = project_root / "windows_app" / "assets" / "app_icon.ico"

    if not source.exists():
        raise FileNotFoundError(f"Ícone PNG não encontrado: {source}")

    with Image.open(source) as image:
        image.save(
            target,
            format="ICO",
            sizes=[(16, 16), (24, 24), (32, 32), (48, 48), (64, 64), (128, 128), (256, 256)],
        )

    print(f"Ícone gerado em: {target}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

